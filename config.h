#ifndef CONFIG_H
#define CONFIG_H

// secrets and config
const char* ssid = "SSID";
const char* password = "PASSWORD";
const char* thingsboardServer = "TB_SERVER";
const int thingsboardPort = 1883; // Puerto predeterminado de MQTT

// Token de acceso de ThingsBoard, generado en la plataforma
const char* token = "DEVICE_TOKEN";

#endif